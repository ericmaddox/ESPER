# Git Push Restriction Rule

Never push any commits, branches, tags, or code to GitHub or any remote repository (e.g. using `git push`, MCP git tools, or shell scripts) unless the user explicitly asks you to in their prompt.
